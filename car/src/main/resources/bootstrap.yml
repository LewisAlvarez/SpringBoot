spring:
  application:
    name: car-service #De igual manera se llama el archivo de configuracion
  cloud:
    config:
      enable: true
      uri: http://localhost:8050 #Ubicacion del servidor de configuracion