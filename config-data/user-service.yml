server:
  port: ${port:0} #De esta manera springboot asigna un puerto aleatorio a la instancia
eureka:
  client:
    fetch-registry: true #Para que sí se registre a el servidor de eureka
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka #Ubicacion del servidor eureka
      hostname: localhost
message: Hola desde usuario